{{#if @isLoading}}
  <div class="loading" aria-busy="true" aria-label="Loading">
    <div class="spinner"></div>
    <p class="loading-text">{{if @message @message "Fetching transactions..."}}</p>
  </div>
{{/if}}
